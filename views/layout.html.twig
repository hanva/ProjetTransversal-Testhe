<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% block page_title %}{% endblock %}
        </title>
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.8/css/all.css" integrity="sha384-3AB7yXWz4OeoZcPbieVW64vVXEwADiYyAEhwilzWsLw+9FgqpyjjStpPnpBO8o8S" crossorigin="anonymous">
        <link rel="stylesheet" href="assets/styles/style.css">
        {% block style %}{% endblock %}
    </head>
    <body>
        <header>
            {% if username is defined %}
                <a href="?action=home">Home</a>
                <a href="?action=addArticle">add an article</a>
                <a href="?action=disconnect">disconnect</a>
            {% endif %}
            {% block nav %}
                <div class="logo">testhé</div>
                <div class="menu">
                    <span class="menu-items">actualithés
                        <img class="little-arrow" src="assets/styles/chevron-bas.png"></span>
                    <span class="menu-items">degusthe
                        <img class="little-arrow" src="assets/styles/chevron-bas.png"></i>
                </span>
                <span class="menu-items">histoire</span>
            </div>
            <div class="menu-right-items">
                <div class="my-account">
                    <i class="far fa-user"></i>
                </div>
                <div class="search-bar">
                    <i class="fas fa-search"></i>
                    {% if username is null %}
                        <button class="register">Register</button>
                        <button class="login">Login</button>
                    {% endif %}
                </div>
            </div>
        {% endblock %}
    </header>
    <div class="blockErrors">
        {{ error }}
        {% for error in errors %}
            <article>
                {{error}}
            </article>
        {% endfor %}
    </div>
    <div class="popup"></div>
    {% block content %}{% endblock %}
    <script src="./assets/script/script.js"></script>
    <script src="./node_modules/jquery/dist/jquery.min.js"></script>
    {% block script %}{% endblock %}
</body>
</html>