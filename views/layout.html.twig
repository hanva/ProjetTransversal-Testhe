<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{% block page_title %}{% endblock %}</title>
    <link rel="stylesheet" href="assets/style.css">
  </head>
  <body>
  {{ error }}
    {% for error in errors %}
          <article>
            {{error}}
          </article>
    {% endfor %}
        <nav>
          {% if username is defined %}
              <a href="?action=home">Home</a>
              <a href="?action=disconnect">disconnect</a>
          {% else %}
              <a href="?action=login">Login</a>
              <a href="?action=createAccount">Sign In</a>
          {% endif %}
        </nav>
    {% block content %}{% endblock %}
  </body>
</html>
